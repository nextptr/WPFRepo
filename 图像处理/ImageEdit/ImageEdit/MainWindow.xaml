<Window x:Class="ImageEdit.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ImageEdit"
        mc:Ignorable="d"
        Title="MainWindow" Height="1000" Width="1000">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="80"/>
        </Grid.ColumnDefinitions>
        <Canvas x:Name="BlackBoard" MouseUp="Image_MouseUp" Width="auto" Height="auto" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Image PreviewMouseMove="Image_MouseMove"  Margin="10" Source="/Image/打标偏斜.jpg" Stretch="Uniform" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            <Line x:Name="lin1" Stroke="Red"   StrokeThickness="1" />
            <Line x:Name="lin2" Stroke="Green" StrokeThickness="3" StrokeDashArray="1 1" />
            <Line x:Name="lin3" Stroke="Blue"  StrokeThickness="2" />
        </Canvas>
        <StackPanel Grid.Column="1">
            <Button Content="Reset" Click="Reset_Click" Height="35" Margin="5" />
        </StackPanel>
    </Grid>
</Window>
