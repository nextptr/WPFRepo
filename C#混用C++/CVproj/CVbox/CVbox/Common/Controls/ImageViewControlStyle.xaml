<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:CVbox.Common.Controls">
    <Style TargetType="{x:Type local:ImageViewControl}" x:Key="{x:Type local:ImageViewControl}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:ImageViewControl}">
                    <GroupBox Header="{TemplateBinding Title}" Margin="2" Width="auto" Height="auto">
                        <Image x:Name="img" Margin="3"/>
                    </GroupBox>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>