<UserControl x:Class="WpfApp.RouteEventUseDemo"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Name="gridRoot" Background="Lime">
        <Grid Name="gridA" Margin="10" Background="Blue">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Canvas Name="canvasLeft" Grid.Column="0" Background="White" Margin="10">
                <Button Name="btn_left" Content="Left" Width="100" Height="100" Margin="100"/>
            </Canvas>
            <Canvas Name="canvasRight" Grid.Column="1" Background="White" Margin="10">
                <Button Name="btn_right" Content="Right" Width="100" Height='100' Margin="100"/>
            </Canvas>
        </Grid>
    </Grid>
</UserControl>
